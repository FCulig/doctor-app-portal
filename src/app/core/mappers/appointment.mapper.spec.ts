import { Appointment.Mapper } from './appointment.mapper';

describe('Appointment.Mapper', () => {
  it('should create an instance', () => {
    expect(new Appointment.Mapper()).toBeTruthy();
  });
});
